# vim: filetype=zsh

# Backup manual del crontab para versionado y restauración

crontab -l > ~/.local/bin/kasumi/state/crontab.bak \
    && echo "[cron]   backup creado correctamente" \
    || echo "[cron]   error al generar backup"
