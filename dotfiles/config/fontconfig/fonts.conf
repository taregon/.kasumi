<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE fontconfig SYSTEM "fonts.dtd">
<fontconfig>
    <!--
    ───────────────────────< MONOSPACE >───────────────────────
    Preferida para terminales y código. -->
    <alias>
        <family>monospace</family>
        <prefer>
            <family>RecMonoSmCasual Nerd Font Mono</family>
            <family>Liberation Mono</family>
        </prefer>
    </alias>
    <!--
    ─────────────────────────< SERIF >─────────────────────────
    Para texto con remates, documentos y lectura prolongada. -->
    <alias>
        <family>serif</family>
        <prefer>
            <family>IBM Plex Serif</family>
        </prefer>
    </alias>
    <!--
    ──────────────────────< SANS-SERIF >─────────────────────
    Para interfaces, web y texto general moderno. -->
    <alias>
        <family>sans-serif</family>
        <prefer>
            <family>UbuntuSans Nerd Font</family>
        </prefer>
    </alias>
    <!--
    ─────────────────────────< EMOJI >─────────────────────────
    Recomendado por múltiples guías para asegurar soporte global. -->
    <alias>
        <family>emoji</family>
        <prefer>
            <family>Noto Color Emoji</family>
        </prefer>
    </alias>
    <!--
    ───────────────────────< IDIOMA / SCRIPT >───────────────────────
    Reglas específicas por idioma para coverage de caracteres. -->
    <!-- Japonés (ja): Agrega Noto Sans CJK JP como fallback principal
       para cubrir caracteres correctamente sin fallbacks indeseados. -->
    <match>
        <test compare="contains" name="lang">
            <string>ja</string>
        </test>
        <edit mode="prepend" name="family">
            <string>Noto Sans CJK JP</string>
        </edit>
    </match>
    <!--
    ────────────────< RENDERIZADO GLOBAL >───────────────────────
    Configuración base para todas las fuentes del sistema. -->
    <match target="font">
        <!-- Hinting: Usa hinting nativo de la fuente (patentes TrueType vencieron) -->
        <edit mode="assign" name="hinting">
            <bool>true</bool>
        </edit>
        <!-- Hintstyle: Moderado, equilibra nitidez y naturalidad -->
        <edit mode="assign" name="hintstyle">
            <const>hintslight</const>
        </edit>
        <!-- Antialiasing: Suavizado de bordes en fuentes escalables -->
        <edit mode="assign" name="antialias">
            <bool>true</bool>
        </edit>
        <!-- Autohint: Desactivado - usa hinting nativo de la fuente -->
        <edit mode="assign" name="autohint">
            <bool>false</bool>
        </edit>
    </match>
    <!--
    ────────────────< EXCEPCIONES >───────────────────────
    Fuentes que requieren configuración especial. -->
    <!-- Noto Color Emoji: Necesita bitmaps embebidos para renderizado correcto -->
    <match target="font">
        <test name="family">
            <string>Noto Color Emoji</string>
        </test>
        <edit mode="assign" name="embeddedbitmap">
            <bool>true</bool>
        </edit>
    </match>
    <!--
    ────────────────< AJUSTES CONDICIONALES >───────────────────────
    Reglas que aplican según propiedades específicas de la fuente. -->
    <!-- Fuentes con peso > medium (negritas): Hinting desactivado -->
    <match target="font">
        <test compare="more" name="weight">
            <const>medium</const>
        </test>
        <edit mode="assign" name="hintstyle">
            <const>hintnone</const>
        </edit>
    </match>
    <!-- Normalizar peso: Si el peso es menor a medium, forzar a regular -->
    <match target="pattern">
        <test compare="less" name="weight" qual="any">
            <const>medium</const>
        </test>
        <edit mode="assign" name="weight">
            <const>regular</const>
        </edit>
    </match>
</fontconfig>
